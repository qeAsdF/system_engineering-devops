#!/usr/bin/env bash
# A Bash script that displays all running processes in a user-oriented format with hierarchy including processes without a TTY.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
